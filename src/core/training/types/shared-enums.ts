/**
 * Shared Enums — Types shared across multiple modules to avoid circular imports.
 *
 * @module shared-enums
 */

/** The three measurable domains of physical capability */
export type BodyDomain = 'MOBILITY' | 'MOTOR_CONTROL' | 'LOAD_TOLERANCE';
