/**
 * Shared Enums — Types shared across multiple modules to avoid circular imports.
 *
 * @module shared-enums
 */

/** The four measurable domains of physical capability */
export type BodyDomain = 'MOBILITY' | 'MOTOR_CONTROL' | 'CAPACITY' | 'POWER';
